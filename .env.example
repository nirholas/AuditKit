# ─────────────────────────────────────────────────────────────
# AuditKit requires ZERO API keys to run.
# All fields below are optional and only increase rate limits.
# ─────────────────────────────────────────────────────────────

# Google PageSpeed Insights (optional)
# Without a key: ~1 req/sec per IP — fine for personal/demo use
# With a key:    higher quota — get one free at console.cloud.google.com
# GOOGLE_API_KEY=

# GitHub (optional — 60 req/hour unauthed is enough for user-triggered audits)
# A token bumps this to 5,000 req/hour
# GITHUB_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
